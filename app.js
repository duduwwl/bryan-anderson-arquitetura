const header=document.querySelector('.site-header');const progress=document.querySelector('.scroll-progress');const menuButton=document.querySelector('.menu-toggle');const mobileMenu=document.querySelector('.mobile-menu');function onScroll(){header?.classList.toggle('is-scrolled',scrollY>30);if(progress){const max=document.documentElement.scrollHeight-innerHeight;progress.style.width=(max>0?scrollY/max*100:0)+'%'}}addEventListener('scroll',onScroll,{passive:true});onScroll();function closeMenu(){if(!mobileMenu||!menuButton)return;mobileMenu.hidden=true;header?.classList.remove('menu-open');menuButton.setAttribute('aria-expanded','false');menuButton.setAttribute('aria-label','Abrir menu');document.body.style.overflow=''}menuButton?.addEventListener('click',()=>{const opening=mobileMenu.hidden;mobileMenu.hidden=!opening;header?.classList.toggle('menu-open',opening);menuButton.setAttribute('aria-expanded',String(opening));menuButton.setAttribute('aria-label',opening?'Fechar menu':'Abrir menu');document.body.style.overflow=opening?'hidden':''});mobileMenu?.querySelectorAll('a').forEach(a=>a.addEventListener('click',closeMenu));addEventListener('keydown',e=>{if(e.key==='Escape')closeMenu()});
document.getElementById('year')?.replaceChildren(String(new Date().getFullYear()));
const motionOkay=!matchMedia('(prefers-reduced-motion: reduce)').matches;
if(motionOkay&&'IntersectionObserver' in window){const observer=new IntersectionObserver(entries=>{for(const entry of entries){if(entry.isIntersecting){entry.target.classList.add('is-visible');observer.unobserve(entry.target)}}},{threshold:.1,rootMargin:'0px 0px -30px 0px'});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el))}else{document.querySelectorAll('.reveal').forEach(el=>el.classList.add('is-visible'))}
const filters=document.querySelectorAll('.filter');filters.forEach(button=>button.addEventListener('click',()=>{const category=button.dataset.filter;filters.forEach(item=>{const active=item===button;item.classList.toggle('active',active);item.setAttribute('aria-pressed',String(active))});document.querySelectorAll('.portfolio-card').forEach(card=>{card.hidden=category!=='todos'&&card.dataset.category!==category})}));
document.getElementById('contact-form')?.addEventListener('submit',event=>{event.preventDefault();const form=event.currentTarget;if(!form.reportValidity())return;const fields=new FormData(form);const message=`Olá, Bryan! Gostaria de conversar sobre um projeto.\n\nNome: ${fields.get('nome')}\nWhatsApp: ${fields.get('whatsapp')}\nE-mail: ${fields.get('email')}\nTipo de projeto: ${fields.get('tipo')}\nCidade: ${fields.get('cidade')}\nMensagem: ${fields.get('mensagem')}`;location.href=`https://wa.me/5535992500889?text=${encodeURIComponent(message)}`});
const galleryItems=[...document.querySelectorAll('.gallery-item')];const lightbox=document.getElementById('lightbox');const lightboxImage=lightbox?.querySelector('.lightbox-image');const lightboxCount=lightbox?.querySelector('.lightbox-count');if(galleryItems.length===1){lightbox?.querySelector('.lightbox-prev')?.setAttribute('hidden','');lightbox?.querySelector('.lightbox-next')?.setAttribute('hidden','')}let currentImage=0;let returnFocus=null;function showImage(index){if(!galleryItems.length||!lightboxImage)return;currentImage=(index+galleryItems.length)%galleryItems.length;const item=galleryItems[currentImage];lightboxImage.src=item.dataset.full;lightboxImage.alt=item.dataset.alt||'';if(lightboxCount)lightboxCount.textContent=`${String(currentImage+1).padStart(2,'0')} / ${String(galleryItems.length).padStart(2,'0')}`};function openLightbox(index){if(!lightbox)return;returnFocus=document.activeElement;showImage(index);lightbox.hidden=false;document.body.style.overflow='hidden';lightbox.querySelector('.lightbox-close')?.focus()};function closeLightbox(){if(!lightbox)return;lightbox.hidden=true;document.body.style.overflow='';lightboxImage?.removeAttribute('src');returnFocus?.focus()};galleryItems.forEach((item,index)=>item.addEventListener('click',()=>openLightbox(index)));lightbox?.querySelector('.lightbox-close')?.addEventListener('click',closeLightbox);lightbox?.querySelector('.lightbox-prev')?.addEventListener('click',()=>showImage(currentImage-1));lightbox?.querySelector('.lightbox-next')?.addEventListener('click',()=>showImage(currentImage+1));lightbox?.addEventListener('click',e=>{if(e.target===lightbox)closeLightbox()});addEventListener('keydown',e=>{if(lightbox?.hidden===false){if(e.key==='Escape')closeLightbox();else if(e.key==='ArrowLeft')showImage(currentImage-1);else if(e.key==='ArrowRight')showImage(currentImage+1);else if(e.key==='Tab'){const controls=[...lightbox.querySelectorAll('button:not([hidden])')];const first=controls[0],last=controls[controls.length-1];if(e.shiftKey&&document.activeElement===first){e.preventDefault();last.focus()}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first.focus()}}}});let touchStartX=0;lightbox?.addEventListener('touchstart',e=>{touchStartX=e.changedTouches[0].screenX},{passive:true});lightbox?.addEventListener('touchend',e=>{const delta=e.changedTouches[0].screenX-touchStartX;if(Math.abs(delta)>50)showImage(currentImage+(delta<0?1:-1))},{passive:true});
